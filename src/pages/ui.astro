---
// import type { Exam } from "../interfaces/exam";
// import Layout from "../layouts/Layout.astro";
// import json from "./soa.json";

// const soa: {
//   question: {
//     texts: string[];
//   };
//   choices: { text: string; correct: boolean }[];
//   explanation: { texts: string[] };
// }[] = json;

// const exam: Exam = {
//   meta: { title: "SOA問題集", language: "en" },
//   questions: soa.map((q) => ({
//     statement: { en: q.question.texts },
//     choices: q.choices.map((choice) => ({
//       en: choice.text,
//     })),
//     corrects: q.choices.reduce<number[]>(
//       (pre, cur, index) => (cur.correct ? [...pre, index + 1] : pre),
//       []
//     ),
//     explanation: { en: q.explanation.texts },
//   })),
// };

// console.log(exam);
// ---

// <Layout title="ui">
//   <pre>{JSON.stringify(exam)}</pre>
// </Layout>
---

<></>
